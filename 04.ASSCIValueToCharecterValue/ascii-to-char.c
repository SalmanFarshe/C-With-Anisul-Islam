#include <stdio.h>
int main()
{
    int asciiVaalue; //a inteser ascii value
    printf("Type your ASCII value : ");
    scanf("%d", &asciiVaalue); // we store a value as a inteser
    printf("The Charecter is : %c", asciiVaalue); // we print the value as a charecter and we are done

    return 0;
}